#!/bin/bash
chmod +x perry
FICHERO=$(basename $1)
NOMBRE="${FICHERO%%.*}"
EXTENSION="${FICHERO#*.}"
if [ "$EXTENSION" != "prr" ]; then
	echo "extensión inválida."
else
	 make --quiet perry
	./perry $1 $2
fi