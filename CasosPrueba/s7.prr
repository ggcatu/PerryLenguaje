//Preuba pointer 

unit test(int @param, bool param2){
	create{
		ptr int point;
	}
	execute{
		free(point);
		return point;
	};
}

main{
	create{
		ptr int x;
		ptr int y;
		int u = 1;
		float o = 3.4;
	} execute{
		u = &x;
 		<< "hahaha";
 	};
}
