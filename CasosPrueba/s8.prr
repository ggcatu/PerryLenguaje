include "input1.prr" 
include "input2.prr" 
include "input3.prr" 
include "p2.prr" 


main{
	create{
		string tomate;
		int cebolla;
		bool tete;
	} execute{
		cebolla = cebolla + cebolla * 3 ;
		tete = cebolla < 3;
		while ( cebolla < 3 ){
			cebolla = 3;
		} ;
		cebolla[cebolla[3]] = cebolla[1] + cebolla[4];
		cebolla[3] = cebolla[1];
 	};
}