//Error tipos en for

type numeritos = int;

int suma(int x, float y, int xx, float yy){
	return x + y;
}

int sumas(array int [3] s){
	return 3;
}
		
bool ssuma(int x, float y){
	return x + y == 0.0;
}

main{
	create{
		float tomate;
		array int [3] s;
	} execute{
		tomate = 1.1;
		ssuma(ssuma(1,2.2),3.3);
 	};
}