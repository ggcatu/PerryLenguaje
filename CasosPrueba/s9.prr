//Prueba type
type numeritos = int;

type union onion{
	char cebolla;
	float cebollaM;
}

main{
	create{
		numeritos bbnum;
		int a;
		float b;
		char c;
		onion p;
	} execute{
		<< p.cebolla + 3;
 		<< 3 + 1 - 3 * 2 / 3 ** 3  ;
 	};
}
