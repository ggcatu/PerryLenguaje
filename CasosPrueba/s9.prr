//Prueba type
type numeritos = int;

union onion{
	int cebolla;
	float cebollaM;
}

main{
	create{
		numeritos bbnum;
		int a;
		float b;
		char c;
		onion p;
		list int lista;
	} execute{
		lista[3] = 3;
		<< p.cebolla + 3;
 		<< 3 + 1 - 3 * 2 / 3 ** 3  ;
 	};
}
